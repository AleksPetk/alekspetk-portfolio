/**
 * project.js — Project detail page helpers (foundation)
 * Reserved for future project-page-specific behavior.
 * Keep empty of speculative features.
 */
(function () {
  "use strict";

  function init() {
    // Intentionally minimal in Phase 0.
    // Future: gallery lightbox hooks, next-project prefetch hints, etc.
    // Only add behavior when a roadmap phase explicitly requests it.
  }

  window.APProject = { init: init };
})();
